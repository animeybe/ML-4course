# Лабораторные работы по предмету
# Нейронные сети и машинное обучение

Интересный предмет, хорошие преподователи 🧠

# Заметки

### ```! Есть несколько веток, но все лабы смотри на ветке main```

#### Работа с git:q
```
git add .
git commit -m "messages"
git push origin main

git checkout -b lab1 // Создание ветки
git checkout lab1 // Переход на ветку

git push -u origin lab1 // Пушим ветку впервые

git checkout main
git merge lab1 // Мерджим ветку в main
git push origin main
```
